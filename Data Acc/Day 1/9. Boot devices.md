We can use a bootable device with a special OS to perform an image
- Set as primary, boot off 

**TOOL : Paladin**
built for linux, but useful for windows
- Auto write block other disks and devices via the GUI

